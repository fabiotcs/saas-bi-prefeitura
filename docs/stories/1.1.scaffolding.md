# Story 1.1: Scaffolding do Projeto e Configuração Base

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [typecheck, lint, vitest]

## Story
**As a** desenvolvedor,
**I want** o projeto Next.js 14 configurado com toda a stack definida no PRD,
**so that** todos os demais módulos possam ser implementados sobre uma base sólida.

## Acceptance Criteria
1. `npm run dev` sobe sem erros em localhost:3000
2. `npm run typecheck` passa sem erros (TypeScript strict mode)
3. `npm run lint` passa sem erros (ESLint + Prettier)
4. `npm run test` executa suite Vitest sem erros
5. Estrutura de pastas conforme PRD criada: `src/app/`, `src/components/`, `src/lib/`, `src/services/`, `src/hooks/`, `src/store/`, `src/types/`
6. `src/lib/api.ts` com instância axios centralizada (interceptors de auth + erro conforme PRD)
7. Prisma conectando ao PostgreSQL sem erros (`npx prisma db push`)
8. TanStack Query v5 configurado com QueryClientProvider no layout root
9. Zustand store base criado em `src/store/`
10. shadcn/ui inicializado com componentes base instalados

## Tasks / Subtasks
- [ ] Criar projeto Next.js 14 com App Router e TypeScript 5+ (AC: 1, 2)
  - [ ] `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir`
  - [ ] Configurar `tsconfig.json` com `strict: true` e path aliases (`@/*`)
- [ ] Instalar e configurar shadcn/ui (AC: 10)
  - [ ] `npx shadcn@latest init`
  - [ ] Instalar componentes: Button, Input, Card, Dialog, Table, Badge, Progress, Select, Form, Tabs, Sheet, Toast, Sonner
- [ ] Criar `src/lib/api.ts` com axios (AC: 6)
  - [ ] Instalar `axios`
  - [ ] Criar AxiosInstance com baseURL, timeout 30s, Content-Type JSON
  - [ ] Interceptor request: injeta `Authorization: Bearer {token}` do localStorage
  - [ ] Interceptor response: redireciona para `/login` em 401
- [ ] Configurar Prisma ORM (AC: 7)
  - [ ] Instalar `prisma` e `@prisma/client`
  - [ ] Criar `prisma/schema.prisma` com provider PostgreSQL
  - [ ] Criar `src/lib/prisma.ts` (singleton client)
- [ ] Configurar TanStack Query v5 (AC: 8)
  - [ ] Instalar `@tanstack/react-query` e `@tanstack/react-query-devtools`
  - [ ] Criar `src/providers/query-provider.tsx` com QueryClient (staleTime 5min)
  - [ ] Envolver layout root com QueryClientProvider
- [ ] Configurar Zustand (AC: 9)
  - [ ] Instalar `zustand`
  - [ ] Criar `src/store/auth.store.ts` (accessToken, user, setUser, logout)
- [ ] Criar estrutura de pastas e arquivos base (AC: 5)
  - [ ] `src/services/.gitkeep`, `src/hooks/.gitkeep`, `src/types/index.ts`
  - [ ] `src/app/(auth)/login/page.tsx` (placeholder)
  - [ ] `src/app/(protected)/dashboard/page.tsx` (placeholder)
- [ ] Configurar scripts `package.json` (AC: 3, 4)
  - [ ] `typecheck`: `tsc --noEmit`
  - [ ] `test`: `vitest`
  - [ ] Instalar `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@vitejs/plugin-react`
  - [ ] Criar `vitest.config.ts`
- [ ] Criar `.env.local` e `.env.example` com variáveis necessárias
- [ ] Criar teste smoke: `src/__tests__/smoke.test.ts` que valida imports principais

## Dev Notes
**Stack:**
- Next.js 14 (App Router) + TypeScript 5 strict
- Tailwind CSS + shadcn/ui
- axios (obrigatório para todas chamadas HTTP — ver PRD)
- Prisma ORM + PostgreSQL
- TanStack Query v5 + Zustand
- Vitest + Testing Library

**Arquivo central axios (src/lib/api.ts) — implementar EXATAMENTE como no PRD:**
```typescript
import axios, { AxiosInstance } from 'axios'
export const api: AxiosInstance = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL,
  timeout: 30_000,
  headers: { 'Content-Type': 'application/json' },
})
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})
api.interceptors.response.use(
  (res) => res,
  (err) => {
    if (err.response?.status === 401) window.location.href = '/login'
    return Promise.reject(err)
  }
)
```

**Variáveis de ambiente necessárias:**
- `NEXT_PUBLIC_API_URL` — URL base da API backend
- `DATABASE_URL` — connection string PostgreSQL

**Nota:** Esta é a story fundação. NENHUMA outra story pode iniciar antes desta estar Done.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-22 | 1.0 | Story criada | @pm/Morgan |
