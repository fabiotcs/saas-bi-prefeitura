# Story 5.4: App Mobile iOS — Scaffolding e Autenticação

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [typecheck, expo-doctor, ios-simulator-test]

## Story
**As a** usuário mobile da prefeitura,
**I want** acessar o sistema pelo iPhone com biometria nativa,
**so that** eu possa usar o sistema em campo sem precisar do computador.

## Acceptance Criteria
1. App Expo roda no simulador iOS sem erros
2. Tela de login com e-mail/senha funcional (mesmos endpoints do web)
3. Captura biométrica via câmera frontal do device funcional
4. Face ID / Touch ID nativos integrados para autenticação rápida (segundo acesso)
5. Tokens armazenados com segurança em `SecureStore` (iOS Keychain)
6. Build IPA gerado via `eas build --platform ios`
7. App submetido ao TestFlight antes da PoC

## Tasks / Subtasks
- [ ] Criar projeto mobile (AC: 1)
  - [ ] `npx create-expo-app mobile --template expo-template-blank-typescript`
  - [ ] Estrutura: `mobile/src/screens/`, `mobile/src/services/`, `mobile/src/components/`, `mobile/src/navigation/`
- [ ] Configurar `mobile/src/services/api.ts` (AC: 2)
  - [ ] mobileApi com axios + AsyncStorage conforme PRD
  - [ ] Instalar `axios`, `@react-native-async-storage/async-storage`
- [ ] Instalar dependências essenciais (AC: 3, 4, 5)
  - [ ] `expo-camera` — câmera frontal para captura biométrica
  - [ ] `expo-local-authentication` — Face ID / Touch ID nativos
  - [ ] `expo-secure-store` — armazenamento seguro de tokens (Keychain iOS)
  - [ ] `expo-router` — navegação file-based
- [ ] Tela de Login (AC: 2)
  - [ ] `mobile/src/app/(auth)/login.tsx`
  - [ ] Campos: e-mail, senha
  - [ ] Chama `POST /auth/login` via mobileApi → challengeToken
  - [ ] Navega para tela de biometria com challengeToken
- [ ] Tela de Biometria (AC: 3, 4)
  - [ ] `mobile/src/app/(auth)/biometric.tsx`
  - [ ] Exibe feed da câmera frontal via `expo-camera`
  - [ ] Botão "Verificar": captura frame, envia para `POST /auth/biometric-verify`
  - [ ] Se accessToken recebido: salvar em `SecureStore`, navegar para home
  - [ ] Botão "Usar Face ID": `LocalAuthentication.authenticateAsync()` (segundo acesso)
- [ ] Armazenamento seguro de tokens (AC: 5)
  - [ ] `mobile/src/services/secure-storage.ts`: `saveTokens`, `getAccessToken`, `clearTokens`
  - [ ] Usar `expo-secure-store` (mapeia para iOS Keychain automaticamente)
- [ ] Navegação base (AC: 1)
  - [ ] `(auth)` group: login, biometric
  - [ ] `(app)` group: tabs com home (placeholder), orders (placeholder)
  - [ ] Redirect: se token válido → app, se não → auth
- [ ] Configuração EAS (AC: 6, 7)
  - [ ] `eas.json` com profiles: development, preview, production
  - [ ] `app.json`: bundleIdentifier `br.gov.aracuai.saasbi`, nome "BI Araçuaí", version "1.0.0"
  - [ ] `eas build --platform ios --profile preview` para TestFlight

## Dev Notes
**⚠️ INICIAR ESTA STORY NA WAVE 1 — em paralelo com EPIC-1.**
Lead time App Store: 2–7 dias úteis. TestFlight pode ser usado na PoC.

**mobileApi (implementar exatamente como no PRD):**
```typescript
import axios from 'axios'
import * as SecureStore from 'expo-secure-store'

export const mobileApi = axios.create({
  baseURL: process.env.EXPO_PUBLIC_API_URL,
  timeout: 30_000,
})

mobileApi.interceptors.request.use(async (config) => {
  const token = await SecureStore.getItemAsync('access_token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})
```

**Face ID no segundo acesso:**
Após o primeiro login completo (senha + biometria via webcam), os acessos subsequentes podem usar `expo-local-authentication` (Face ID / Touch ID). Isso é mais rápido e mais conveniente em campo.

**Checklist de submissão App Store (ver EPIC-5):**
- Apple Developer Account USD 99/ano
- Bundle ID registrado
- Screenshots obrigatórias
- Política de privacidade (URL)

**API_URL**: apontar para o mesmo backend do web (`EXPO_PUBLIC_API_URL` em `.env`)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-22 | 1.0 | Story criada | @pm/Morgan |
