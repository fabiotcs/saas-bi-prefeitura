# Story 1.3: Schema do Banco — Módulos Auth, Usuários e Auditoria Base

## Status
Draft

## Executor Assignment
executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: [prisma-validate, migration-test, trigger-test]

## Story
**As a** desenvolvedor,
**I want** o schema completo do banco para Auth, Usuários e Auditoria,
**so that** todas as stories subsequentes do EPIC-1 tenham estrutura persistente desde o início.

## Acceptance Criteria
1. `npx prisma migrate dev` executa sem erros com todos os modelos
2. Trigger PostgreSQL bloqueia UPDATE e DELETE em `audit_logs` — retorna erro
3. Trigger PostgreSQL bloqueia UPDATE e DELETE em `biometric_records` — retorna erro
4. Seed cria usuário MAIN_MANAGER funcional com email/senha de teste
5. Todos os índices de performance criados nas colunas de filtro frequente
6. Migration reversível (down migration documentada)

## Tasks / Subtasks
- [ ] Modelos Prisma — Autenticação (AC: 1)
  - [ ] `User`: id(uuid), fullName, birthDate, phone, email(unique), cpf(unique), rg, role(enum), secretaryId(nullable), photoUrl, lastLogin, biometricVerified(bool), documentVerified(bool), passwordHash, createdAt, updatedAt
  - [ ] `Session`: id(uuid), userId(FK), refreshToken(unique), expiresAt, ipAddress, userAgent, createdAt
  - [ ] `TwoFactorRequest`: id(uuid), userId(FK), action(enum), otpHash, expiresAt, used(bool), createdAt
- [ ] Modelos Prisma — Auditoria imutável (AC: 2, 3)
  - [ ] `AuditLog`: id(uuid), action, urlPath, userId(FK nullable), userAgent, ipAddress, status(enum), metadata(Json nullable), occurredAt
  - [ ] `BiometricRecord`: id(uuid), userId(FK), capturedImageUrl, similarityScore(Float), livenessScore(Float), confidenceLevel(Float), fraudAlertLevel(enum), aiEstimatedAge(Int nullable), ipAddress, status(enum), createdAt
- [ ] Enums Prisma (AC: 1)
  - [ ] `UserRole`: MAIN_MANAGER, SECRETARY_MANAGER, SECRETARY_USER, AUDIT_VIEWER
  - [ ] `AuditStatus`: SUCCESS, FAILED, SUSPICIOUS
  - [ ] `FraudAlertLevel`: NONE, LOW, MEDIUM, HIGH
  - [ ] `BiometricStatus`: SUCCESS, FAILED, SUSPICIOUS
  - [ ] `TwoFactorAction`: APPROVE_ORDER, SUBMIT_COMMITMENT, SUBMIT_ADDITIVE
- [ ] Migration e triggers de imutabilidade (AC: 2, 3)
  - [ ] Criar migration: `npx prisma migrate dev --name foundation-schema`
  - [ ] SQL customizado na migration: trigger `prevent_audit_log_modification` em `audit_logs`
  - [ ] SQL customizado na migration: trigger `prevent_biometric_record_modification` em `biometric_records`
  - [ ] Triggers devem usar `RAISE EXCEPTION` com mensagem clara
- [ ] Índices de performance (AC: 5)
  - [ ] `@@index([userId])` em AuditLog e BiometricRecord
  - [ ] `@@index([status])` em AuditLog e BiometricRecord
  - [ ] `@@index([occurredAt])` em AuditLog
  - [ ] `@@index([createdAt])` em BiometricRecord
  - [ ] `@@index([email])` em User (além do unique)
- [ ] Seed (AC: 4)
  - [ ] `prisma/seed.ts`: criar usuário admin (email: admin@aracuai.mg.gov.br, role: MAIN_MANAGER, senha: hash bcrypt)
  - [ ] `package.json`: adicionar script `"prisma": { "seed": "ts-node prisma/seed.ts" }`
- [ ] Testes de trigger (AC: 2, 3)
  - [ ] Teste: `DELETE FROM audit_logs WHERE id = ?` deve lançar exceção PostgreSQL
  - [ ] Teste: `UPDATE biometric_records SET status = ? WHERE id = ?` deve lançar exceção

## Dev Notes
**Triggers de imutabilidade (SQL puro na migration):**
```sql
CREATE OR REPLACE FUNCTION prevent_audit_log_modification()
RETURNS TRIGGER AS $$
BEGIN
  RAISE EXCEPTION 'audit_logs records are immutable. DELETE and UPDATE are forbidden.';
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER audit_logs_immutable
BEFORE UPDATE OR DELETE ON audit_logs
FOR EACH ROW EXECUTE FUNCTION prevent_audit_log_modification();
```
Replicar para `biometric_records`.

**CRÍTICO para PoC (Item 40):** A imutabilidade dos logs deve ser garantida no banco, não apenas na API. O endpoint DELETE/PATCH da API retorna 403, mas mesmo que alguém acesse o banco diretamente, o trigger bloqueia.

**Sobre passwordHash:** Usar `bcrypt` com saltRounds=12. Nunca armazenar senha em texto.

**Nota:** Esta story deve ser concluída ANTES de 1.4, 1.5, 1.6, 1.7 e 1.8.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-22 | 1.0 | Story criada | @pm/Morgan |
