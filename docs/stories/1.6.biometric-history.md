# Story 1.6: Histórico de Autenticações Biométricas

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [typecheck, lint, vitest]

## Story
**As a** auditor ou gestor de segurança,
**I want** visualizar o histórico completo de autenticações biométricas,
**so that** qualquer tentativa de acesso seja rastreável e auditável.

## Acceptance Criteria
1. `GET /api/auth/biometric-history` retorna lista paginada com filtros
2. Histórico exibe imagem capturada miniaturizada (thumbnail)
3. Filtros por status (SUCCESS/FAILED/SUSPICIOUS) funcionais
4. Filtros por período (from/to) funcionais
5. Tentativa de `DELETE /api/auth/biometric-history/:id` retorna 403
6. Somente perfis MAIN_MANAGER e AUDIT_VIEWER podem acessar o histórico

## Tasks / Subtasks
- [ ] Implementar `src/services/biometric-history.service.ts` (AC: 1)
  - [ ] `listBiometricHistory(params)` via axios GET `/auth/biometric-history`
- [ ] `GET /api/auth/biometric-history` (AC: 1, 3, 4)
  - [ ] Filtros: userId, status, from, to
  - [ ] Paginação: page, limit (default 50)
  - [ ] Guard: somente MAIN_MANAGER e AUDIT_VIEWER (AC: 6)
- [ ] `DELETE /api/auth/biometric-history/:id` → 403 (AC: 5)
- [ ] Hook `useBiometricHistory` com TanStack Query (AC: 1)
- [ ] Página `/audit/biometric` (AC: 2, 3, 4)
  - [ ] Tabela com colunas: thumbnail da imagem capturada, usuário (nome + foto), similarityScore (%), livenessScore (%), fraudAlertLevel (badge colorido), IP, status (badge), data/hora
  - [ ] Filtro de status (Select): ALL / SUCCESS / FAILED / SUSPICIOUS
  - [ ] Date pickers para filtro de período
  - [ ] Paginação na tabela
- [ ] Testes (AC: 1, 5, 6)
  - [ ] Teste: DELETE retorna 403
  - [ ] Teste: SECRETARY_USER não acessa `/audit/biometric`

## Dev Notes
**Cores dos badges:**
- SUCCESS: verde
- FAILED: vermelho
- SUSPICIOUS: amarelo/laranja

**fraudAlertLevel badges:**
- NONE: cinza
- LOW: azul
- MEDIUM: amarelo
- HIGH: vermelho

**Thumbnail da imagem:** A `capturedImageUrl` está no S3. Usar `<img>` com `object-fit: cover` em 48x48px.

**Nota:** O trigger no banco já impede DELETE/UPDATE. O 403 na API é a segunda camada de proteção.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-22 | 1.0 | Story criada | @pm/Morgan |
