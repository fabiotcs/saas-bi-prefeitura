# Story 1.4: Autenticação JWT + Biometria Facial

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [typecheck, lint, vitest, security-review]

## Story
**As a** usuário do sistema,
**I want** fazer login com e-mail/senha seguido de verificação biométrica facial,
**so that** apenas eu, com meu rosto confirmado, possa acessar o sistema.

## Acceptance Criteria
1. `POST /api/auth/login` com credenciais válidas retorna `{ challengeToken }` (JWT 5min)
2. `POST /api/auth/login` com credenciais inválidas retorna 401
3. `POST /api/auth/biometric-verify` com faceImage + challengeToken válido retorna `{ accessToken, refreshToken }`
4. Login sem biometria (apenas login/senha) não concede acesso ao sistema
5. accessToken expira em 15 minutos; refreshToken expira em 7 dias
6. `POST /api/auth/refresh` renova accessToken com refreshToken válido
7. `POST /api/auth/logout` invalida refreshToken no banco
8. Cada tentativa biométrica (sucesso ou falha) registrada em `BiometricRecord`
9. IP do dispositivo registrado em cada registro biométrico e de sessão
10. Componente `<BiometricCapture>` abre webcam e captura frame ao clicar em "Verificar"

## Tasks / Subtasks
- [ ] Instalar dependências de auth (AC: 1, 5)
  - [ ] `bcrypt`, `@types/bcrypt`
  - [ ] `jose` (JWT para Edge runtime do Next.js)
- [ ] `POST /api/auth/login` (AC: 1, 2)
  - [ ] `src/app/api/auth/login/route.ts`
  - [ ] Validar email + senha com bcrypt.compare
  - [ ] Se válido: emitir challengeToken (JWT HS256, payload: userId, exp: 5min)
  - [ ] Registrar tentativa em AuditLog
- [ ] Provider biométrico — **DECISÃO:** usar `face-api.js` (client-side) ou AWS Rekognition (server-side via axios)
  - [ ] Criar `src/lib/biometric-provider.ts` com interface `BiometricProvider`
  - [ ] Implementar `AwsRekognitionProvider` (axios para `rekognition.amazonaws.com`) OU `FaceApiProvider` (face-api.js)
  - [ ] **Recomendado para PoC:** face-api.js (sem dependência externa paga)
- [ ] `POST /api/auth/biometric-verify` (AC: 3, 4, 8, 9)
  - [ ] `src/app/api/auth/biometric-verify/route.ts` — aceita multipart/form-data
  - [ ] Validar challengeToken JWT
  - [ ] Chamar biometricProvider.verify(faceImage, documentImage?)
  - [ ] Se sucesso: emitir accessToken (15min) + refreshToken (7d), salvar Session no banco
  - [ ] Sempre registrar BiometricRecord (sucesso ou falha) com IP
- [ ] `POST /api/auth/refresh` (AC: 6)
  - [ ] Validar refreshToken no banco (não expirado, não revogado)
  - [ ] Emitir novo accessToken
- [ ] `POST /api/auth/logout` (AC: 7)
  - [ ] Marcar refreshToken como revogado (deletar Session)
  - [ ] Registrar em AuditLog
- [ ] Componente `<BiometricCapture>` (AC: 10)
  - [ ] `src/components/auth/BiometricCapture.tsx`
  - [ ] Solicita permissão de câmera via `navigator.mediaDevices.getUserMedia`
  - [ ] Exibe feed da webcam em `<video>`
  - [ ] Botão "Verificar": captura frame via `<canvas>`, converte para Blob
  - [ ] Exibe resultado: sucesso (✅) / falha (❌) / suspeito (⚠️)
- [ ] Página de login integrada (AC: 3, 4)
  - [ ] `src/app/(auth)/login/page.tsx` — Passo 1: form email/senha → Passo 2: BiometricCapture
  - [ ] Após biometria aprovada: salva tokens, redireciona `/dashboard`
- [ ] Testes (AC: 1, 2, 4, 5)
  - [ ] Teste unit: login com credenciais válidas retorna challengeToken
  - [ ] Teste unit: login com credenciais inválidas retorna 401
  - [ ] Teste unit: refreshToken expirado retorna 401

## Dev Notes
**Fluxo completo de auth (2 etapas):**
```
1. POST /api/auth/login → { challengeToken } (JWT 5min, assina com JWT_SECRET)
2. POST /api/auth/biometric-verify → multipart:
     - challengeToken: string
     - faceImage: Blob (frame da webcam)
     - documentImage?: Blob (RG/CNH — obrigatório no 1º acesso)
   → { accessToken, refreshToken, user }
```

**JWT_SECRET** deve estar em `.env.local` como `JWT_SECRET=<random-256-bit-hex>`.

**Para face-api.js (recomendado para PoC):**
- Usar modelos: `ssd_mobilenetv1` (detecção) + `face_recognition_net` (embedding)
- Modelos devem ser servidos de `public/models/`
- Comparação no servidor: calcular euclidean distance entre embeddings (< 0.6 = match)

**IP:** Extrair de `request.headers.get('x-forwarded-for')` ou `request.ip` no Next.js.

**⚠️ CRÍTICO:** Nenhuma rota protegida deve ser acessível sem JWT válido. O middleware (Story 1.2) valida o accessToken em cada request.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-22 | 1.0 | Story criada | @pm/Morgan |
