# Story 1.8: Gestão de Usuários — CRUD + Perfis de Acesso

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [typecheck, lint, vitest]

## Story
**As a** gestor principal,
**I want** cadastrar, visualizar e gerenciar usuários com perfis de acesso distintos,
**so that** cada colaborador tenha acesso adequado ao sistema.

## Acceptance Criteria
1. Formulário com 6 campos obrigatórios: fullName, birthDate, phone, email, cpf, rg
2. CPF com máscara XXX.XXX.XXX-XX e validação de dígito verificador
3. E-mail único — sistema retorna erro amigável se duplicado
4. Listagem exibe: foto, nome, e-mail, perfil (badge), último login, status biométrico
5. Filtros por nome e e-mail funcionais na listagem
6. 4 perfis de acesso com permissões distintas e demonstráveis: MAIN_MANAGER, SECRETARY_MANAGER, SECRETARY_USER, AUDIT_VIEWER
7. AUDIT_VIEWER ao logar é redirecionado para `/audit/viewer` (painel restrito TCE/MP)
8. Upload de foto de perfil funcional

## Tasks / Subtasks
- [ ] Implementar `src/services/user.service.ts` completo (AC: 1–5)
  - [ ] `listUsers`, `createUser`, `getUserById`, `updateUser` conforme PRD
- [ ] `GET /api/users` com filtros search e email (AC: 4, 5)
- [ ] `POST /api/users` com validação CPF e e-mail único (AC: 1, 2, 3)
- [ ] `GET /api/users/:id` e `PATCH /api/users/:id` (AC: 4)
- [ ] `POST /api/users/:id/photo` — upload foto para S3 (multipart, AC: 8)
- [ ] Validações (AC: 1, 2, 3)
  - [ ] CPF: máscara + algoritmo de dígito verificador (módulo 11)
  - [ ] E-mail: formato RFC5322 + unicidade no banco
  - [ ] birthDate: data válida, usuário maior de 18 anos
- [ ] Página `/users` — listagem (AC: 4, 5)
  - [ ] Tabela: avatar 40x40, nome, e-mail, perfil (badge colorido), último login formatado, biometricVerified (ícone), ações (editar)
  - [ ] Input de busca por nome (debounce 300ms)
  - [ ] Select de filtro por e-mail
- [ ] Página `/users/new` e `/users/:id/edit` — formulário (AC: 1, 2, 6)
  - [ ] Campos: fullName, birthDate (date picker), phone (máscara), email, CPF (máscara), RG, role (select com descrição), secretaryId (select, aparece se role != MAIN_MANAGER)
  - [ ] Upload de foto com preview
- [ ] Página `/audit/viewer` — painel restrito AUDIT_VIEWER (AC: 7)
  - [ ] Layout sem sidebar de navegação geral
  - [ ] Apenas links: Logs de Auditoria, Histórico Biométrico
  - [ ] Redirecionamento automático pós-login para AUDIT_VIEWER
- [ ] Guards de rota por perfil (AC: 6)
  - [ ] Hook `useRequireRole(roles: UserRole[])` — redireciona se perfil insuficiente
  - [ ] SECRETARY_USER não acessa `/users`, `/audit`, `/budget`
  - [ ] SECRETARY_MANAGER acessa apenas sua secretaria
- [ ] Testes (AC: 2, 3, 6)
  - [ ] CPF inválido retorna erro de validação
  - [ ] E-mail duplicado retorna 409
  - [ ] AUDIT_VIEWER redirecionado para `/audit/viewer`

## Dev Notes
**Badges de perfil:**
- MAIN_MANAGER: roxo (admin)
- SECRETARY_MANAGER: azul
- SECRETARY_USER: verde
- AUDIT_VIEWER: cinza (somente leitura)

**Algoritmo de validação CPF:**
```typescript
function isValidCPF(cpf: string): boolean {
  const digits = cpf.replace(/\D/g, '')
  if (digits.length !== 11 || /^(\d)\1+$/.test(digits)) return false
  // Calcular 1º dígito verificador
  let sum = 0
  for (let i = 0; i < 9; i++) sum += parseInt(digits[i]) * (10 - i)
  let check = (sum * 10) % 11
  if (check === 10 || check === 11) check = 0
  if (check !== parseInt(digits[9])) return false
  // Calcular 2º dígito verificador
  sum = 0
  for (let i = 0; i < 10; i++) sum += parseInt(digits[i]) * (11 - i)
  check = (sum * 10) % 11
  if (check === 10 || check === 11) check = 0
  return check === parseInt(digits[10])
}
```

**Upload de foto:** `POST /api/users/:id/photo` recebe `multipart/form-data` com campo `photo`. Redimensionar para 200x200px antes de salvar no S3 (usar `sharp`).

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-22 | 1.0 | Story criada | @pm/Morgan |
