# Story 4.1: Schema do Banco — Módulos 12, 14 e 16

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [typecheck, lint, vitest]

## Story
**As a** desenvolvedor,
**I want** os novos modelos Prisma para Estabelecimentos, Configurações e Integrações,
**so that** as features de Rede Credenciada, Configurações do Sistema e API Keys tenham persistência.

## Acceptance Criteria
1. Migration executa sem erros
2. `Establishment` com campos name, cnpj, address, city, state, lat, lng, phone, email, servicesDescription, ordersCompleted, rating, status, isFavorite (por usuário via EstablishmentFavorite)
3. `SystemSettings` como tabela singleton (um registro por instância)
4. `ApiKey` com campos name, key (hash), permissions[], lastUsedAt, revoked

## Tasks / Subtasks
- [x] Schema Prisma: `Establishment` + enum `EstablishmentStatus`
- [x] Schema Prisma: `EstablishmentFavorite` (userId + establishmentId)
- [x] Schema Prisma: `SystemSettings` (singleton)
- [x] Schema Prisma: `ApiKey` + `ApiCallLog`
- [x] Migration executada sem erros
- [x] Typecheck limpo

## File List
- `prisma/schema.prisma`
- `prisma/migrations/20260323123824_epic4_gestao2/migration.sql`

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-23 | 1.0 | Story criada | @dev/Dex |
